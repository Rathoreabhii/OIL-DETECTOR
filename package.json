{
  "name": "maritime-environment",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cinematic 3D maritime environment: spectral FFT ocean, Mumbai-like coastline, wind/current simulation state.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
